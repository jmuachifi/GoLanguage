package main

import "fmt"

func main() {
	var i, j string = "Hello", "World"

	fmt.Println(i)
	fmt.Println(j)

	/*
		If we want to print the arguments in new lines,
		we need to use \n. Example:

		var i, j, string = "Hello", "World"
		fmt.Println(i, "\n")
		fmt.Println(j, "\n")
	*/
}
